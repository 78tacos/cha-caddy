# Copy to .env and fill in. Never commit .env.

# Optional. Grok lookup, Find tea info, polish notes, generated photos.
XAI_API_KEY=

# Optional. Postgres connection string. If unset, Cha Caddy uses a local PGLite file.
DATABASE_URL=

# Keep true so sign-in stays on.
VITE_AUTH_ENABLED=true
